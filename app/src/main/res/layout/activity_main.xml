<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white">

     <!-- project from https://github.com/jpardogo/PagerSlidingTabStrip -->
    <com.astuetz.PagerSlidingTabStrip
        android:id="@+id/main_tabs"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/accountingPrimary"
        app:pstsIndicatorColor="@android:color/white"
        app:pstsTabTextSize="20sp"
        app:pstsTabTextColor="@drawable/tab_text_color_selector"
        app:pstsTabTextAllCaps="false"
        app:pstsShouldExpand="true"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/main_viewpager"
        android:layout_width="match_parent"
        android:layout_height="fill_parent" />

</LinearLayout>